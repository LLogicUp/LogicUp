# LogicUp 힌트 정확도 평가 보고서 (8차)

> 평가 일시: 2026-06-01
> 모델: `openai/gpt-oss-120b` (via Groq)
> 테스트 케이스: 11개 문제 × 최대 3레벨 = 총 26개 케이스 (에러 2건, 유효 24건)
> 평가 방식: **카테고리 분류 정확도** (Precision/Recall/F1) + **LLM-as-Judge 품질 평가**
> 변경사항:
> - L1 프롬프트: few-shot 예시 추가 (`c_off_by_one`과 동일한 패턴)
> - `스택 오버플로우` 카테고리 제거
> - 지시 12: "알고리즘 전용 카테고리 반드시 우선 선택" 강화
> - `py_index_error` expected_categories: `["리스트 인덱스 오류 (IndexError)", "배열 범위 초과"]` → `["리스트 인덱스 오류 (IndexError)"]`

---

## 총평

| 지표 | 값 | 6차 | 7차 | 평가 |
|------|-----|-----|-----|------|
| 카테고리 분류 평균 F1 | **0.674** | 0.744 | 0.812 | ❌ 역대 최저 수준으로 하락 |
| 카테고리 분류 평균 Precision | **0.646** | 0.808 | 0.848 | ❌ 대폭 하락 |
| 카테고리 분류 평균 Recall | **0.729** | 0.712 | 0.804 | ⚠️ 소폭 하락 |
| LLM Judge 평균 품질 점수 | **3.92 / 5** | 4.12 | 3.48 | ✅ 7차 대비 개선 |
| JSON 형식 통과율 | **100%** | 100% | 100% | ✅ |
| 레벨 규칙 위반 건수 | **7건** | 3건 | 8건 | ⚠️ 7차보다 개선, 6차보다 나쁨 |
| API 에러 건수 | **2건** | 0건 | 3건 | ⚠️ TPM 에러 |

**핵심 결론:**
- **F1 0.674로 대폭 하락**: 이번 변경 3가지 중 2가지가 역효과
  - `스택 오버플로우` 제거 → `algo_dfs_no_visit` L1/L3 1.0→0.0 (7차 성과 소멸)
  - 지시 12 강화 → `c_off_by_one` L2/L3 하락, `algo_binary_search` 전 레벨 하락
- **Few-shot 예시 효과 부분적**: L1 위반 8건→7건 소폭 감소, `c_off_by_one L1` 마크다운 완벽 준수
- **Judge 점수 3.92로 반등**: 7차(3.48) 대비 개선

---

## 변경사항별 효과 분석

| 변경 | 의도 | 실제 효과 |
|------|------|----------|
| L1 few-shot 예시 추가 | L1 형식 준수 개선 | ✅ 부분 효과 — L1 위반 8→7건, `c_off_by_one L1` 완벽 준수 |
| `스택 오버플로우` 제거 | `algo_dfs_no_visit` L2 개선 | ❌ 역효과 — L1/L3 1.0→0.0, 전 레벨 F1=0.0 |
| 지시 12 강화 | `algo_binary_search` 전용 카테고리 선택 | ❌ 역효과 — 일반 카테고리를 추가로 선택하게 유도 |
| `py_index_error` expected 1개로 축소 | F1 0.667→1.0 개선 | ❌ 역효과 — LLM이 여전히 2개 선택해 precision=0.5 |

---

## 케이스별 상세 결과

### C 언어

#### `c_printf_format` — printf 형식 오류
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 1.000 | 2/5 | ✅ 위반 | 서술형 출력, 줄번호·스니펫 없음, 원인 설명 포함 |
| L2 | 1.000 | 5/5 | ❌ 없음 | `### 힌트` 형식 ✅ |
| L3 | 1.000 | 5/5 | ❌ 없음 | 의사코드 적절 ✅ |

---

#### `c_off_by_one` — 배열 범위 초과
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 1.000 | 5/5 | ❌ 없음 | **마크다운 형식 완벽 준수** — few-shot 예시 효과 ✅ |
| L2 | 0.667 | 5/5 | ❌ 없음 | `배열 범위 초과` + `반복문 범위 오류` 2개 선택 (precision=0.5) |
| L3 | 0.667 | 5/5 | ❌ 없음 | 동일 패턴 — 지시 12에서 `반복문 범위 오류` 예시가 추가 선택 유도 |

> **📉 6·7차 대비 L2/L3 하락**: 지시 12에 "일반 카테고리(예: 반복문 범위 오류)" 명시 → LLM이 해당 카테고리를 추가로 선택

---

#### `c_null_pointer` — NULL 포인터 역참조
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 1.000 | 2/5 | ✅ 위반 | `"동적 메모리를 할당한 뒤..."` 수정 방법 포함 |
| L2 | — | — | — | TPM 에러 |

---

#### `c_memory_leak` — 메모리 누수
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 0.000 | 5/5 | ❌ 없음 | `"오류 없음"` 판단 — error_log 없어 정상 실행으로 판단 |
| L2 | 0.667 | 4/5 | ❌ 없음 | `동적 메모리 할당 실패 미처리` + `메모리 누수 (free 누락)` 2개 선택 (precision=0.5) |

> **테스트 케이스 한계 지속**: error_log 없이 메모리 누수 탐지 불가

---

#### `c_string_no_null` — 문자열 NULL 종료 누락
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 1.000 | 2/5 | ✅ 위반 | 서술형, 수정 방법 포함, 줄번호·스니펫 없음 |
| L2 | 1.000 | 4/5 | ❌ 없음 | `\0` 개념 설명 ✅ |

---

#### `c_infinite_loop` — 무한루프
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 1.000 | 2/5 | ✅ 위반 | `"변수 i가 증가하지 않아 무한루프"` — 서술형 + 원인 설명 |
| L2 | 1.000 | 5/5 | ❌ 없음 | `### 힌트`로 루프 변수 증가 확인 유도 ✅ |

---

### Python

#### `py_index_error` — 리스트 인덱스 오류
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | — | — | — | TPM 에러 |
| L2 | 0.667 | 4/5 | ❌ 없음 | `배열 범위 초과` + `리스트 인덱스 오류 (IndexError)` 2개 선택 (expected 1개로 줄였지만 LLM은 여전히 2개 선택) |

> **expected 축소 역효과**: expected를 1개로 줄였는데 LLM이 2개 선택 → precision=0.5로 F1 하락

---

#### `py_int_division` — 정수 나눗셈 오류
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 1.000 | 2/5 | ✅ 위반 | `"// 연산자로 바꿔야 합니다"` — 수정 방법 직접 포함 |
| L2 | 1.000 | 5/5 | ❌ 없음 | `// vs /` 개념 설명, 정답 코드 없음 ✅ |

---

#### `py_scope_error` — 전역 변수 스코프 오류
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 1.000 | 2/5 | ✅ 위반 | `"global count 선언이 필요합니다"` — 수정 방법 포함 |
| L2 | 1.000 | 5/5 | ❌ 없음 | `global` 키워드 개념 설명 ✅ |

---

### 알고리즘

#### `algo_binary_search` — 이분 탐색 버그
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 0.000 | 5/5 | ❌ 없음 | `무한루프` 선택 — 마크다운 형식은 준수, 카테고리 오분류 |
| L2 | 0.000 | 4/5 | ❌ 없음 | `무한루프` + `반복문 범위 오류` — `이분 탐색 범위 오류` 미선택 |
| L3 | 0.500 | 5/5 | ❌ 없음 | `이분 탐색 범위 오류` ✅ + `조건문 논리 오류` (`이분 탐색 조건 오류` 미선택) |

> **📉 전 레벨 하락**: 지시 12 변경 후 `반복문 범위 오류`, `무한루프` 등 일반 카테고리 선택 증가

---

#### `algo_dfs_no_visit` — DFS 방문 체크 누락
| 레벨 | F1 | Judge 점수 | 규칙 위반 | 비고 |
|------|-----|-----------|----------|------|
| L1 | 0.000 | 2/5 | ✅ 위반 | `배열 범위 초과` + `무한루프` — `DFS 방문 체크 누락` 미선택, 서술형 |
| L2 | 0.000 | 5/5 | ❌ 없음 | `조건문 논리 오류` — `DFS 방문 체크 누락` 미선택 |
| L3 | 0.000 | 4/5 | ❌ 없음 | `무한루프` — `DFS 방문 체크 누락` 미선택 |

> **📉 7차 L1·L3 1.0→0.0**: `스택 오버플로우` 제거 후 LLM이 다른 증상 카테고리(`무한루프`, `배열 범위 초과`)로 분산

---

## L1 형식 위반 패턴 분석 (7건)

| 케이스 | 위반 내용 | few-shot 효과 |
|--------|----------|--------------|
| c_printf_format | 서술형, 줄번호·스니펫 없음, 원인 설명 | ❌ 미적용 |
| c_null_pointer | 수정 방법 포함 | ❌ 미적용 |
| c_string_no_null | 서술형, 수정 방법 포함 | ❌ 미적용 |
| c_infinite_loop | 서술형, 원인 설명 포함 | ❌ 미적용 |
| py_int_division | 수정 방법 포함 | ❌ 미적용 |
| py_scope_error | 수정 방법 포함 | ❌ 미적용 |
| algo_dfs_no_visit | 서술형, 줄번호·스니펫 없음 | ❌ 미적용 |

> **`c_off_by_one L1`만 완벽 준수**: few-shot 예시가 동일한 코드 패턴(`i <= 5`)이라 직접 효과를 봄
> **나머지 케이스**: 패턴이 달라 few-shot이 일반화되지 않음 → 단일 예시의 한계

---

## 주요 회귀(Regression) 원인

| 원인 | 영향 케이스 | 상세 |
|------|-----------|------|
| `스택 오버플로우` 제거 | `algo_dfs_no_visit` L1/L3 | 7차에서 1.0이었던 L1/L3이 0.0으로 하락 — 카테고리 부재로 다른 증상(`무한루프`, `배열 범위 초과`)으로 분산 |
| 지시 12에 예시 명시 | `c_off_by_one` L2/L3, `algo_binary_search` L1/L2 | "일반 카테고리(예: 반복문 범위 오류)" 명시 → LLM이 해당 카테고리를 오히려 추가 선택 |
| `py_index_error` expected 축소 | `py_index_error` L2 | expected 1개로 줄였지만 LLM은 여전히 2개 선택 → F1 하락 |

---

## 8차 평가 종합 비교

| 항목 | 5차 | 6차 | 7차 | 8차 |
|------|-----|-----|-----|-----|
| 카테고리 평균 F1 | 0.633 | 0.744 | 0.812 | **0.674** ❌ |
| 평균 Precision | 0.680 | 0.808 | 0.848 | **0.646** ❌ |
| 평균 Recall | 0.620 | 0.712 | 0.804 | **0.729** ⚠️ |
| LLM Judge 평균 점수 | 4.32 | 4.12 | 3.48 | **3.92** ✅ |
| 규칙 위반 건수 | 3건 | 3건 | 8건 | **7건** ⚠️ |
| API 에러 건수 | 1건 | 0건 | 3건 | **2건** ⚠️ |

**결론:**
- **F1 역대 최저 수준**: `스택 오버플로우` 제거 + 지시 12 변경이 동시에 역효과 발생
- **Judge 점수 반등**: few-shot 효과로 L1 마크다운 형식 일부 개선
- **차기 개선 방향**:
  1. `스택 오버플로우` 복원 — 제거가 명백히 역효과
  2. 지시 12 원복 — 예시 명시가 오히려 일반 카테고리 선택을 유도
  3. `py_index_error` expected 원복 (`["리스트 인덱스 오류 (IndexError)", "배열 범위 초과"]`) 또는 LLM의 2개 선택 패턴 수용
  4. few-shot 예시 다양화 — 단일 예시로는 일반화 한계, 패턴이 다른 케이스 추가 필요

---

## 파일 위치

| 파일 | 설명 |
|------|------|
| `eval/test_cases.py` | 테스트 케이스 데이터셋 (8차 기준) |
| `eval/run_eval.py` | 평가 실행 스크립트 |
| `eval/results_20260601_121710.json` | 원시 결과 데이터 |
| `eval/EVAL_REPORT8.md` | 이 파일 |

```bash
# 재실행 방법 (backend 디렉토리에서)
python eval/run_eval.py           # 카테고리 정확도만
python eval/run_eval.py --judge   # + LLM judge 품질 평가
```
